<Page
    x:Class="UnoApp1.Presentation.CartPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoApp1.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utu="using:Uno.Toolkit.UI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <utu:NavigationBar Content="Giỏ hàng" Grid.Row="0" />

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding CartItems}" 
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="{Binding ProductImageUrl}" Height="80" Stretch="Uniform"/>

                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ProductName}" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Foreground="Red">
                                <Run Text="{Binding UnitPrice}" />
                                <Run Text=" đ" />
                            </TextBlock>

                            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,5,0,0">
                                <Button Content="-" 
                                        Width="30" Height="30" Padding="0"
                                        Command="{Binding DataContext.DecreaseQuantityCommand, ElementName=PageRoot}"
                                        CommandParameter="{Binding}"/>

                                <TextBlock Text="{Binding Quantity}" 
                                           VerticalAlignment="Center" 
                                           FontWeight="Bold"
                                           FontSize="16"/>

                                <Button Content="+" 
                                        Width="30" Height="30" Padding="0"
                                        Command="{Binding DataContext.IncreaseQuantityCommand, ElementName=PageRoot}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="2" 
                                Content="Xóa" 
                                Background="Red" Foreground="White"
                                Command="{Binding DataContext.RemoveItemCommand, ElementName=PageRoot}"
                                CommandParameter="{Binding}"
                                VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="2" Background="#F0F0F0">
            <StackPanel Padding="20,20,20,40">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Tổng cộng: " FontWeight="Bold" FontSize="18"/>
                    <TextBlock Text="{Binding TotalPrice}" FontWeight="Bold" FontSize="18" Foreground="Red"/>
                    <TextBlock Text=" đ" FontSize="18" Foreground="Red"/>
                </StackPanel>

                <Button Content="TIẾN HÀNH ĐẶT HÀNG" 
                        Command="{Binding GoToCheckoutCommand}"
                        HorizontalAlignment="Stretch" 
                        Margin="0,0,0,5"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
